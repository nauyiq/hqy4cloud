logging:
  config: classpath:logback.xml
server:
  port: 9527
spring:
  profiles:
     active: dev
  application:
    name: gateway-service
  cloud:
    loadbalancer:
      ribbon:
        enabled: false

