logging:
  config: classpath:logback.xml

server:
  port: 8088
spring:
  application:
    name: account-auth-service
  profiles:
    active: test

#暴露所有
management:
  endpoints:
    web:
      exposure:
        include: "*"
