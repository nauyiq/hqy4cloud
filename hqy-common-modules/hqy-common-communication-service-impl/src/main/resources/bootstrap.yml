logging:
  config: classpath:logback.xml

server:
  port: 8777

spring:
  application:
    name: common-communication-service
  profiles:
    active: dev

management: #暴露所有
  endpoints:
    web:
      exposure:
        include: "*"